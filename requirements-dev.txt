# Build/verification tooling (NOT the app runtime). ASCII only (see requirements.txt note).
-r requirements.txt
pyinstaller==6.22.2     # pinned: reproducible build
numpy                   # tools/make_icon.py, tools/check_icon.py — linear-light icon resample
pillow                  # tools/make_icon.py, tools/check_icon.py — image handling
